# How to install

Clone the repo and its dependencies

```sh
git clone --recurse-submodules https://github.com/donatomartin/zsh.git ~/.config/zsh
```
Source the entrypoint

```sh
source ~/.config/zsh/init.zsh
```
